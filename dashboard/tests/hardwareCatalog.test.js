'use strict';
const { getCatalog, validateHardwareConfiguration } = require('../services/hardwareCatalogService');
test('catalog is draft and unqualified',()=>{const c=getCatalog();expect(c.schema).toBe(1);expect(c.qualification).toBe('draft');expect(c.boards[0].qualification).not.toBe('qualified');});
test('valid config remains unverified',()=>{const c=getCatalog(),b=c.boards[0];const r=validateHardwareConfiguration({schema:1,boardId:b.id,boardVersion:b.version,modules:[{instanceId:'screen1',typeId:'display.generic-i2c',catalogVersion:'1',bus:{type:'i2c',id:'i2c0',address:60},parameters:{width:128,height:64}}]});expect(r.valid).toBe(true);expect(r.readiness).toBe('configured_unverified');expect(r.applyEligible).toBe(false);});
test('reserved duplicate and unknown rejected',()=>{const b=getCatalog().boards[0];const r=validateHardwareConfiguration({schema:1,boardId:b.id,boardVersion:b.version,modules:[{instanceId:'a',typeId:'display.generic-spi',catalogVersion:'1',extra:1,pins:{cs:17}},{instanceId:'b',typeId:'display.generic-spi',catalogVersion:'1',pins:{cs:17}}]});expect(r.valid).toBe(false);expect(r.errors.map(x=>x.code)).toEqual(expect.arrayContaining(['unknown_field','pin_reserved','pin_conflict']));});

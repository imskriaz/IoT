#pragma once

// Legacy optional example only.
// Runtime config should come from dashboard onboarding and NVS, not from this file.
// credentials.h is git-ignored — never commit real credentials.

// ── Device Identity ───────────────────────────────────────────────────────────
#define DEVICE_ID        ""

// ── SIM / Cellular ────────────────────────────────────────────────────────────
#define APN              ""

// ── MQTT Broker ───────────────────────────────────────────────────────────────
#define MQTT_HOST        ""
#define MQTT_PORT        1883
#define MQTT_USER        ""
#define MQTT_PASS        ""

// Local WiFi (for same-network direct HTTP access)
#define WIFI_SSID        ""
#define WIFI_PASSWORD    ""

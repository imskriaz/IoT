1. Timezone in http://localhost:3001/settings add all options and selectoble, in ui everywhere for all even use that timezone with sync with system local 

3. Fix restart dashboard path

4. In dashboard add pull messages
5. In adnroid registation no need to show MQTT or HTTP option as app making using both and no need to show mqtt setting as it take from dashboard only. Logic is where whta best we use mqtt or http , even we use websocket, logic will be fallback also so device never be in offline but in best connection, For realtime data over  
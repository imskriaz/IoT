1. Timezone in http://localhost:3001/settings add all options and selectoble, in ui everywhere for all even use that timezone with sync with system local 

2. Notification same one show multiple time

3. Fix restart dashboard path

4. In dashboard add pull messages

5. In android app app, in call and sms screen like dashboard add device selection option at header where on click show device and sims, sims if avaiable, once selected show Device and Sim Number as header with SMS or Call and show that device sms or call and send, rcvd map to that device. In that way android app also can use to send sms or receive from from another device. 

6. In android app, on click call, it again open phone dialer, rather direct call. 